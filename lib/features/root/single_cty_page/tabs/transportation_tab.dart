import 'package:visit_uzbekistan/widget_imports.dart';

class TransportationTab extends StatelessWidget {
  const TransportationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Transportation tab');
  }
}
