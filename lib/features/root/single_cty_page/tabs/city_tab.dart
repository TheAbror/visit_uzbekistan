import 'package:visit_uzbekistan/widget_imports.dart';

class CityTab extends StatelessWidget {
  const CityTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('City tab');
  }
}
